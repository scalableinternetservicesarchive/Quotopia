//alert("destroyed!");
$('.favorite-<%=@favorite_quote.quote_id%>').html(
    "<%= j render(
        :partial => "favorite_quotes/favorite_block",
        :locals => {
            :favorite => nil,
            :quote_id => @favorite_quote.quote_id, 
            :current_user_id => @favorite_quote.user_id
        }
    ) %>");
