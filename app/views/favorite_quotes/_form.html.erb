<%= form_for(@favorite_quote) do |f| %>
  <% if @favorite_quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite_quote.errors.count, "error") %> prohibited this favorite_quote from being saved:</h2>

      <ul>
      <% @favorite_quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quote_id %><br>
    <%= f.text_field :quote_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
